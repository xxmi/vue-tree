import Vue from 'vue';
import Router from 'vue-router';
import {VTree, VSelectTree} from '@/components/index.js';

Vue.use(VTree);
Vue.use(VSelectTree);


Vue.use(Router);

export default new Router({
  routes: []
});
